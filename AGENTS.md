# 项目协作与版本管理规则

## 语言与称呼

- 始终优先使用中文回复。
- 称呼项目负责人为“艾卡”。
- 先给结论，再说明依据、改动和验证结果。

## Claude Code、Codex 与 GitHub 协作流程

1. Codex 先输出经艾卡确认的设计规格与分批实施计划。
2. Claude Code 严格按当前批次计划实现代码，并运行计划指定的测试。
3. Claude Code 完成后不得自行推送 GitHub；应提供改动文件、测试结果、偏离计划之处和遗留风险。
4. Codex 对 Claude Code 的实际改动进行代码审查，并独立运行与改动相关的自动化测试、必要的短 epoch 训练及界面测试。
5. 只有 Codex 明确给出“审查通过/验收通过”结论后，才允许由 Codex提交并推送到艾卡指定的 GitHub 仓库。
6. 审查未通过时不得上传版本；先形成问题清单，由 Claude Code 或 Codex按艾卡安排修复，再重新验收。
7. Claude Code 只负责业务代码实现和对应测试，不参与 README、路线图、规格、实施计划或其他项目文档的创建与修改。
8. 项目文档、发布说明、GitHub 仓库整理与版本上传由 Codex 负责，并由艾卡审核方向。

## GitHub 操作边界

- 指定公开远端仓库：`https://github.com/luZhaoHao/Auto_labeltrain_project`；公开状态已由艾卡于 2026-08-14 明确批准。
- 除非艾卡后续明确变更，不得向其他远端仓库或镜像推送本项目内容。
- 仅管理源码、测试、脱敏配置模板和必要研发文档。
- 不提交数据集、图片、标注文件、模型权重、训练产物、日志、审计运行文件、上传缓存、虚拟环境或构建产物。
- 不提交真实 `auto_tune/config.yaml`、API 密钥、Token、密码或其他凭据；只允许提交脱敏的配置模板。
- 推送前必须检查 `.gitignore`、待提交文件清单和敏感信息，并运行相关测试。
- 禁止未经艾卡明确要求执行强制推送、重写远端历史、删除远端分支或公开仓库。
- 每个通过验收的批次使用独立、可解释的版本提交；提交说明应描述功能范围和验证结果。

## 磁盘空间约束

- 本项目因磁盘空间有限，不使用包含数据集、权重和训练结果的本地多版本副本或工作树。
- 本地 Git 只跟踪轻量源码和文档；大文件始终保留在版本控制之外。
- 测试产生的临时文件应使用受控临时目录；不得为了版本管理复制整个项目。

## 文档版本与归档规则

- `docs/` 只保留当前仍作为研发、交接、路线或实施依据的权威版本；同一文档的新旧版本不得长期并列，以免误用过期结论。
- 新版文档完成内容核对和必要的渲染验收后，旧版优先移动到项目根目录 `历史文档/`，不直接删除；需要永久删除历史文档时必须先取得艾卡单独确认。
- 归档前必须核对文件名、日期、替代关系、Git 状态以及项目内引用入口，确认新版已经完整承接旧版仍然有效的事实、边界和验收证据。
- `历史文档/` 中的文件默认只读，仅用于追溯，不作为新批次设计、编码或验收依据；若历史文档与当前权威文档冲突，以当前交接记录、路线图、实施计划及艾卡最新批准为准。
- 完成每个研发批次后，Codex 负责同步更新交接记录、路线图、实施计划、批次规格及研发执行版 DOCX，并检查是否产生需要归档的旧版本；自 2026-08-21 起不再要求同步生成 PDF，已有过期 PDF 按归档规则处理。
- 开始新批次前必须检查 `docs/` 是否仍存在过期或重复版本；发现后先完成归档和引用修正，再进入下一批开发。
- 文档移动、归档和删除不得夹带业务代码、数据集、权重、训练产物、日志或其他无关文件。

## 代码修改与测试

- 不擅自新增依赖、删除文件或执行破坏性操作。
- 修改代码后必须运行与改动相关的测试；声称完成前必须提供测试证据。
- 真实训练优先使用最小合法数据集和短 epoch，除非艾卡明确要求完整训练。
- 正式数据接入流程使用目录选择；ZIP 仅作为遗留兼容路径处理。

## Python 与 Conda 环境规则

- 任何 Python、pytest、项目脚本、数据处理或临时验证命令都必须在明确指定的 Conda 环境中运行，禁止直接使用系统 Python、WindowsApps Python 或来源不明的解释器。
- 交互式终端应先执行 `conda activate <环境名>`；自动化或非交互命令可使用 `conda run -n <环境名> ...`，或使用该 Conda 环境中 `python.exe` 的绝对路径，确保运行时实际绑定同一环境。
- 运行代码或测试前应检查环境名、`sys.executable` 和 Python 版本；测试报告必须记录所用 Conda 环境或解释器绝对路径。
- 本项目当前代码与测试统一使用 `auto_tune` Conda 环境；不得改用系统默认 Python。非交互 PowerShell 可使用 `D:\Program Files\anaconda3\envs\auto_tune\python.exe`。
- 如果艾卡没有说明应使用哪个 Conda 环境，必须先询问艾卡；仅在任务确实需要隔离环境且范围明确时才可创建专用测试环境，并说明环境名、用途和新增依赖，禁止静默创建或复用不明环境。

## 下次开发入口

- 开始新批次前必须先读取 `docs/development_handoff_20260814.md`。
- 当前已发布稳定版本为 `v0.2`；A0 审计闭环、统一训练收尾和 Studio S1.1 已发布。Studio S1.2 与 S1.3 已于 2026-08-21 完成独立验收但尚未提交，按艾卡要求在文档回写完成后统一进入提交检查。
- 后续产品方向读取 `docs/roadmap_20260814.md`，具体任务顺序读取 `docs/implementation_plan_20260814.md`。

# Auto-Tune Studio 后续实施计划（2026-09-02 修订）

> 本文只维护当前获批的 Studio 未完成批次、执行顺序、工作量和验收门槛。Research 与 Cloud 的长期方向见 `roadmap_20260814.md`，待分别立项后再建立实施计划；当前已完成事实与测试基线见 `development_handoff_20260814.md`。

## 1. 执行结论

Q1 已完成代码实现与 Codex 独立验收，当前等待艾卡完成线下体验后再提交、推送。后续按 **H1 → C1 → C2 → F1 → S4** 顺序实施。每次只启动一个经艾卡确认的小批次；开始编码前为该批次建立独立规格和可执行任务计划，完成后再更新交接、路线与本文。

| 批次 | 工作量 | 前置条件 | 退出门槛 |
|---|---:|---|---|
| H1 Detect HPO MVP | 6–10 人日 | Q1 完成 | 固定预算可完成、恢复、比较并确定性选优 |
| C1 Classify 直接训练 | 7–12 人日 | H1 完成且公共接口稳定 | 最小真实分类数据可完成训练、历史与报告闭环 |
| C2 Classify LLM/HPO | 4–7 人日 | C1 完成 | 两种调优均使用 Classify 专用事实和规则 |
| F1 双任务冻结 | 3–5 人日 | C2 完成 | 双任务、三策略完成集成回归和真实短训练 |
| S4 Windows/单 Docker 镜像/API 交付 | 17–29 人日 | F1 冻结 | Windows 可直接安装；同一镜像的 Server/Engine 模式均可部署、调用和保留数据 |

## 2. Q1：LLM 可信度 MVP（已完成，待线下体验与提交）

**目标**：让“模型可说错、系统不执行错”成为可测试约束。

交付范围：

- 由代码生成版本化事实包，包含来源 ID、值、缺失状态、任务类型和运行绑定。
- 为 LLM 决策建立严格 Schema，限制动作、参数、证据引用和理由字段。
- 确定性校验指标方向、best/final 语义、缺失值、参数类型/范围/依赖、任务适用性和单轮修改数量。
- 校验失败时允许一次结构化纠错；再次失败则记录终态并禁止启动训练。
- 最终改善判断、优胜参数和停止条件继续由代码计算。

首版不做：第二模型复核、知识图谱、多 Agent 辩论、HPO 结果交给 LLM 裁决。

完成记录：Q1.1 事实与证据契约、Q1.2 确定性语义校验、同一事实包的一次纠错、逐次决策审计和审计失败即关闭均已实现。2026-09-02 Codex 独立验收为完整套件 **1484 passed / 2 warnings / 0 skipped**；合法语义决策完成一次最小 Detect CUDA 短训练，三类非法语义决策共六次模型响应均为零命令构造、零训练启动。语义规则和维护边界见 `llm_tuning_semantic_rules.md`。

## 3. H1：Detect HPO MVP（6–10 人日）

**引擎选择**：产品使用 Optuna TPE，随机搜索作为基线；Ultralytics 遗传调优只做内部基准比较。

首版流程：

1. 用户在直接训练、HPO、LLM 三种策略中选择一种。
2. HPO 创建独立 study，绑定数据快照、模型、目标指标、预算和搜索空间版本。
3. 每个 trial 复用现有训练执行、状态、日志、产物和失败终态。
4. 首版搜索约 6 个高价值参数：`lr0`、`lrf`、`optimizer`、条件化 `momentum`、`weight_decay`、`warmup_epochs`。
5. 按固定 trial 数顺序执行；超时、OOM、用户停止和非法组合形成明确终态。
6. 使用统一指标确定性排名，候选最优参数可再做一次固定配置验证训练。

首版不做：Ray、并行调度、多目标优化、复杂剪枝、自动扩大搜索空间。

验收门槛：TPE 与随机基线均可运行；study 可恢复；失败 trial 不污染优胜结果；同一指标口径可回溯到原始产物；无 LLM 凭据时 HPO 完整可用。

## 4. C1：YOLOv8 Classify 直接训练（7–12 人日）

交付范围：分类目录与快照校验、模型/任务命令、训练启动与停止、Top-1/Top-5 指标、日志与状态、统一历史、报告、页面展示和结构化错误。

复用公共运行设施，但独立定义 Classify 数据规则、指标解析和参数白名单。首版不接入 LLM/HPO，以降低问题定位复杂度。

验收门槛：最小合法数据集真实短训练成功；非法目录、空类别、训练失败、停止、刷新恢复和历史查看均通过；Detect 回归不受影响。

## 5. C2：Classify LLM/HPO（4–7 人日）

交付范围：Classify 专用事实画像、决策 Schema 规则、参数白名单、Optuna 搜索空间、目标指标和确定性排名；复用 Q1/H1 的通用编排与审计接口。

验收门槛：Classify 的 LLM 和 HPO 均可独立运行；Detect 专属字段不能进入 Classify；无效建议或 trial 不启动/不计入优胜；三种策略结果均进入统一历史。

## 6. F1：双任务冻结（3–5 人日）

- 完整自动化回归。
- Detect、Classify 分别执行直接训练、LLM、HPO 的代表性路径。
- 两个任务各完成最小真实短训练和失败/停止/恢复验证。
- 固化依赖、配置模板、数据协议和用户操作说明。
- 冻结后才允许进入跨平台封装，避免容器化期间继续变更核心业务接口。

## 7. S4：Windows、单 Docker 镜像与 API 交付（17–29 人日）

| 子项 | 工作量 | 主要内容 |
|---|---:|---|
| 统一任务 API 与服务内核 | 4–6 人日 | `/api/v1/jobs`，覆盖分析、三种训练策略、结果分析、状态、日志、停止与产物查询 |
| Windows 直接安装 | 4–7 人日 | 安装包、运行依赖、配置、启动入口、升级/卸载和数据保留 |
| 单 Docker 镜像双模式 | 4–7 人日 | 同一镜像的 Server/Engine 启动模式、GPU/CPU 运行、健康检查与持久化目录 |
| 平台接口与安全边界 | 3–5 人日 | 鉴权、输入输出协议、受控目录/NAS、错误码、幂等与调用示例 |
| 集成交付验收 | 2–4 人日 | Windows、Server、Engine 三种入口的双任务短训练、重启恢复和文档核对 |

### 7.1 单镜像运行方式

- 只发布一个 `auto-tune` Docker 镜像，并以一个容器承载所选运行模式。
- `AUTO_TUNE_MODE=server`：注册完整 Web UI 与统一 API，允许同一网络通过 IP/端口登录操作。
- `AUTO_TUNE_MODE=engine`：不注册 UI 路由，不加载模板与静态资源，只保留统一 API，供公司大平台调用。
- 数据集分析、直接训练、HPO、LLM 调优和训练结果分析复用同一任务内核，不为 Engine 复制业务实现。
- 挂载 GPU 时使用 NVIDIA 运行时；未挂载 GPU 时允许 CPU 回退，不发布独立 CPU/CUDA 镜像。
- 数据集、模型、结果和配置通过受控目录或 NAS 挂载；容器中不得固化密钥、业务数据、权重或训练结果。

### 7.2 首版明确不做

- 第二个 Docker 镜像或 Web/Worker 多容器拆分。
- Linux 原生安装包和 Python Wheel 对外交付。
- 将整套 Studio 安装进公司大平台；平台只通过 Engine 模式 API 集成。
- 多租户、集群调度、计费、高可用和复杂分布式任务编排。

## 8. 通用质量与发布门槛

- 每批必须通过相关单元/集成测试、完整回归和必要的最小真实训练。
- LLM/HPO 的输入、候选、校验、实际参数、命令、指标和终态必须可追溯。
- 新依赖必须单独说明用途、版本与部署影响，经艾卡确认后再引入。
- 每批完成后更新三份权威 Markdown 和研发执行版 DOCX；过期批次文件移动到 `历史文档/`，不直接删除。
- GitHub 提交前检查 `.gitignore`、文件清单、敏感信息和测试证据；只推送指定公开仓库。

## 9. 估算口径与风险

剩余批次总计 **37–63 人日**。估算基于一名熟悉现有代码的研发人员，包含设计、实现、自动测试、短训练、界面验证、Windows 与容器交付验证和文档回写；不包含 GPU/外部资源等待、采购或部署审批、重大上游依赖不兼容和新增任务类型。若 Classify 输入约定、公司平台接口或目标 GPU 容器环境较晚确定，应取区间上限并预留联调时间。
